# FreshPage V0 极简验证版方案

> 文档状态：已按“无需登录、首页输入网址、直接展示结果”重新设计  
> 建议开发周期：2～4 周  
> 建议人员：1 名有经验的全栈开发者，配合 AI 开发与人工测试  
> 本阶段目标：验证陌生用户是否认可一次性网站内容体检的价值

---

## 1. V0 产品定义

FreshPage V0 是一个无需注册的网站内容体检工具。

用户打开首页，只需要输入网站网址并点击“开始检查”。后台自动发现和检查网站页面，完成后直接在当前页面展示结果。

完整流程只有四步：

```text
打开首页 → 输入网站网址 → 等待后台扫描 → 查看检测结果
```

用户不需要：

- 注册账号；
- 登录；
- 安装代码；
- 连接 CMS；
- 填写公司资料；
- 配置复杂规则；
- 提供邮箱；
- 添加支付方式。

### 一句话承诺

> 输入网站地址，几分钟内找出可能过期、互相矛盾或已经失效的网页内容。

---

## 2. V0 要验证的核心假设

V0 只验证以下问题：

1. 陌生用户是否愿意把网站地址提交给 FreshPage；
2. 用户能否理解扫描结果；
3. 系统能否发现至少一个用户认可的真实问题；
4. 用户是否愿意把结果分享给同事或立即修改网站；
5. 用户是否会主动提出持续监控、更多页面或导出报告的需求。

V0 不验证账户体系、订阅付费、团队协作或持续监控。

---

## 3. 目标用户

### 首选用户

拥有 10～100 个公开页面的小型 SaaS 创始人、独立开发者、产品经理和网站负责人。

典型网站包含：

- 首页；
- 价格页；
- 功能页；
- 帮助文档；
- 博客；
- 联系方式；
- 服务条款或退款政策。

### V0 适合扫描的网站

- 无需登录即可访问；
- 中文或英文；
- 页面总量较小；
- 主要内容可以通过 HTML 或浏览器渲染获得；
- 最近一年修改过产品、价格、品牌或政策。

### V0 暂不适合

- 大型电商平台；
- 新闻媒体；
- 页面数量超过 500 的内容网站；
- 主要内容必须登录才能访问的网站；
- 小程序或原生 App；
- 强烈依赖地区、身份或个性化展示的网站；
- 要求正式法律、SEO、无障碍或安全合规结论的客户。

---

## 4. 用户体验流程

## 4.1 首页

首页首屏只保留：

- 产品名称和一句话说明；
- 一个网址输入框；
- “开始检查”按钮；
- 简短说明：“无需登录，最多检查 25 个公开页面”；
- 一个示例报告入口；
- 隐私和抓取说明链接。

建议首页文案：

> **你的网站内容还准确吗？**  
> 输入网址，检查失效链接、过期日期、价格冲突和可能已经过时的内容。

输入框占位文案：

> https://example.com

### 提交前端校验

- 自动补全缺失的 `https://`；
- 只接受 HTTP 或 HTTPS 地址；
- 明确提示格式错误；
- 用户连续点击时只创建一次扫描；
- 提交后立即进入进度状态。

## 4.2 扫描进度

不跳转到需要登录的后台。首页主体切换为扫描进度。

显示四个阶段：

1. 正在确认网站是否可以访问；
2. 正在发现网站页面；
3. 正在检查内容和链接；
4. 正在整理结果。

同时显示：

- 已发现页面数；
- 已检查页面数；
- 最多检查 25 页；
- 当前阶段；
- 大致等待提示；
- “取消检查”按钮。

不要向普通用户展示队列、worker、DOM、HTTP 客户端等技术术语。

## 4.3 结果页面

扫描完成后，在同一产品中打开结果页。结果页通过不可预测的扫描 ID 访问，例如：

```text
/result/{random-scan-id}
```

结果页无需登录，但不能使用连续数字 ID。链接有效期为 7 天。

页面顶部展示：

- 被扫描的网站；
- 扫描时间；
- 已检查页面数量；
- 检查是否完整；
- 严重问题、建议检查、正常项目的数量；
- “重新检查”按钮；
- “复制结果链接”按钮。

### 结果分组

1. **优先处理**：确定性高、可能直接影响客户的问题；
2. **建议检查**：疑似过期或矛盾，需要人工确认；
3. **页面概览**：所有成功和失败页面；
4. **本次检查限制**：未扫描页面和无法判断的内容。

## 4.4 问题详情

每条结果卡片至少包含：

- 问题标题；
- 严重程度；
- 所在页面；
- 原文证据；
- 为什么被报告；
- 推荐的人工操作；
- “有帮助”与“不是问题”反馈按钮。

若问题涉及两个页面，必须并排展示双方原文，不能只展示 AI 总结。

---

## 5. V0 扫描范围

### 5.1 页面上限

- 每次最多扫描 25 个公开页面；
- 默认链接遍历深度为 2；
- 首页和疑似价格、功能、帮助、条款页面优先；
- 超出上限时在报告中明确说明未扫描页数；
- 不允许用户在 V0 修改页面上限。

### 5.2 页面发现顺序

1. 用户提交的 URL；
2. `robots.txt` 中声明的 sitemap；
3. 常见 sitemap 地址；
4. sitemap 中的高价值页面；
5. 首页和已扫描页面中的站内链接；
6. 达到 25 页后停止。

### 5.3 页面优先级

URL 或标题包含以下语义时优先：

- pricing、price、plans、套餐、价格；
- features、product、功能、产品；
- help、docs、support、帮助、文档；
- refund、cancel、terms、退款、取消、条款；
- about、contact、关于、联系。

博客文章可以补充扫描，但不能挤掉价格页、功能页和政策页。

### 5.4 默认排除

- 图片、音频、视频、字体和压缩包；
- 下载文件；
- 搜索结果页；
- 标签、日历和无限分页；
- 登出、删除、提交表单等操作地址；
- 明显的会话、追踪和广告参数；
- 外部域名页面；
- robots.txt 禁止访问的页面；
- 需要认证的页面。

---

## 6. V0 检测内容

V0 检测分为确定性检查和 AI 辅助检查。

## 6.1 确定性检查

这些问题不需要 AI 判断。

### A. 页面访问异常

- 提交的网址无法访问；
- 页面返回 4xx 或 5xx；
- 重定向次数过多；
- 出现重定向循环；
- 最终跳转到其他域名；
- 页面加载超时。

临时超时和 5xx 至少重试一次，不能首次失败就宣称页面已经失效。

### B. 失效站内链接

- 已扫描页面中的站内链接返回 4xx 或 5xx；
- 同一个失效目标出现在多个页面时合并为一条；
- 展示失效目标和所有来源页面；
- 不检查页面中的每一个外部链接，避免扫描失控。

### C. 明确过期日期候选

检测：

- 已经过期的活动日期；
- 已过去的报名或促销截止日期；
- 旧年份与“今年、最新、即将、限时”等表述共同出现；
- 页面标题或主要 CTA 中出现已过去的明确日期。

历史博客中正常出现旧日期，不直接视为问题。

### D. 联系信息异常

- 同一网站出现多个不同的主要联系电话；
- 联系邮箱域名与当前网站品牌明显不一致；
- `mailto:` 或 `tel:` 内容与页面显示值不一致；
- 联系链接本身失效。

只报告候选，不宣称某个联系方式一定错误。

### E. 基础内容异常

- 页面标题为空；
- H1 为空；
- 页面几乎没有可读取正文；
- 页面正文包含明显占位文本，如 lorem ipsum；
- 生产页面出现常见测试环境提示；
- 关键页面包含明显未替换模板变量。

## 6.2 跨页面事实候选

系统从页面抽取以下内容：

- 金额和币种；
- 月付、年付或一次性周期；
- 免费试用天数；
- 使用额度、人数、容量和百分比；
- 产品名、套餐名和品牌名；
- 日期与截止时间；
- 退款或取消期限；
- 功能是否包含、免费或即将推出。

系统按“实体 + 周边语境”进行分组，例如：

```text
Pro + monthly + $29
Pro + monthly + $39
```

当同一个实体在多个页面出现不同明确值时，生成“疑似不一致”候选。

以下情况不能直接判为冲突：

- 月付与年付；
- 不同币种；
- 不同国家或地区；
- 限时促销；
- 历史发布文章；
- 起步价与固定价；
- 示例数据；
- 不同产品使用了相同套餐名。

## 6.3 AI 辅助判断

AI 只处理已经由规则发现的少量候选，不读取整站后自由发挥。

AI 的任务：

- 判断两段内容是否疑似描述同一个事项；
- 识别月付、年付、地区、促销和历史语境；
- 将技术结果解释为普通用户能理解的语言；
- 给出需要人工确认的具体问题。

AI 输出必须包含：

- `is_suspected_issue`；
- `confidence`；
- `summary`；
- `evidence_a`；
- `evidence_b`；
- `possible_explanation`；
- `recommended_check`。

AI 不得：

- 从互联网搜索所谓正确答案；
- 宣称某价格或政策一定错误；
- 自动修改网站；
- 没有引用页面原文就输出问题；
- 把低置信度判断列为严重问题。

## 6.4 严重程度

### 严重

- 首页或价格页不可访问；
- 关键站内链接确定失效；
- 明显未替换的模板变量；
- 高置信度的价格或退款期限冲突。

### 建议检查

- 疑似过期日期；
- 疑似价格、试用期或额度不一致；
- 联系信息不一致；
- 可能仍在使用的旧名称；
- 页面正文异常少。

### 正常或信息

- 页面成功访问；
- 未发现明显问题；
- 页面因 robots.txt 未扫描；
- 因 25 页上限未覆盖。

---

## 7. 误报控制

V0 是否有价值主要取决于误报数量。

必须采取以下措施：

- 导航和页脚中的重复问题合并；
- 同一个失效链接只创建一条结果；
- 价格比较必须保留币种和计费周期；
- 日期判断必须结合周边语境；
- 博客发布时间不当作过期内容；
- 相同数字但语义不同不得比较；
- AI 低置信度结果默认不展示；
- AI 服务失败时仍展示确定性结果；
- “建议检查”不能使用“错误”“违规”等确定语气；
- 每次扫描最多展示 20 条主要问题，其余折叠；
- 用户的“不是问题”反馈需要被记录。

建议目标：每个网站首页报告中，无意义结果不超过 3 条。

---

## 8. 前端状态设计

首页必须处理完整状态，不得只有理想流程。

### 初始状态

- 网址输入框；
- 开始检查按钮；
- 示例报告；
- 检查范围说明。

### 提交中

- 禁用重复提交；
- 显示正在创建任务；
- 超过数秒仍无响应时给出重试入口。

### 扫描中

- 阶段式进度；
- 已处理页面数；
- 用户可以复制当前结果链接；
- 刷新页面后仍能恢复进度；
- 任务失败时显示可理解原因。

### 扫描完成

- 结果摘要；
- 问题列表；
- 页面覆盖；
- 结果反馈；
- 重新检查；
- 复制链接。

### 部分完成

如果部分页面失败：

- 仍然展示已有结果；
- 明确说明成功和失败页数；
- 不将整个扫描标记为失败；
- 允许重试失败页面。

### 完全失败

分别说明：

- 网站无法访问；
- robots.txt 不允许；
- 网站要求登录；
- 网站阻止自动访问；
- 地址不安全；
- 系统临时故障。

---

## 9. 匿名结果与数据保留

由于 V0 不登录，需要专门设计匿名访问方式。

### 结果访问

- 每次扫描生成不可预测的随机 ID；
- 扫描 ID 至少具有足够随机性，不能被枚举；
- 结果链接仅凭该随机 ID 访问；
- 页面明确提醒：“任何拥有此链接的人都可以查看结果”；
- 不在结果中展示抓取页面以外的任何用户数据。

### 数据保留

- 扫描结果默认保存 7 天；
- 页面正文和截图到期后自动删除；
- 只保留匿名统计指标；
- 用户可以在结果页立即删除本次扫描；
- 删除后结果链接永久失效；
- 日志中不长期保存完整网页正文。

### 防滥用

- 按 IP、设备或网络特征限制扫描频率；
- 同一域名短时间内复用正在进行的扫描；
- 使用验证码或风险验证处理异常请求；
- 限制并发扫描数；
- 设置每日全局页面预算；
- 对反复失败或异常域名进行临时冷却；
- 不能依赖前端限制，后台必须强制配额。

---

## 10. 极简数据模型

V0 不需要 User、Workspace、Membership、Subscription 等实体。

### Scan

- id：内部 ID；
- public_token：结果页随机标识；
- submitted_url；
- normalized_domain；
- status；
- current_stage；
- discovered_count；
- scanned_count；
- failed_count；
- created_at；
- started_at；
- finished_at；
- expires_at；
- deleted_at；
- abuse_fingerprint_hash。

### PageResult

- id；
- scan_id；
- requested_url；
- final_url；
- normalized_url；
- http_status；
- title；
- h1；
- normalized_text；
- content_hash；
- screenshot_path；
- page_type；
- crawl_status；
- error_code；
- fetched_at。

### ExtractedFact

- id；
- scan_id；
- page_result_id；
- fact_type；
- entity_key；
- raw_text；
- normalized_value；
- unit；
- qualifier；
- context_text；
- confidence。

### Finding

- id；
- scan_id；
- finding_type；
- fingerprint；
- severity；
- title；
- summary；
- page_result_ids；
- evidence_json；
- recommendation；
- confidence；
- detection_method；
- helpful_count；
- not_helpful_count；
- created_at。

### ScanEvent

- id；
- scan_id；
- event_type；
- stage；
- progress_value；
- safe_message；
- created_at。

用于刷新后恢复进度和排查任务问题，不保存敏感内容。

---

## 11. 技术架构

```text
首页与结果页
      │
      ▼
Web/API 应用
  ├─ URL 校验
  ├─ 创建匿名扫描
  ├─ 查询进度
  ├─ 返回结果
  └─ 接收结果反馈
      │
      ▼
任务队列 ── 扫描 Worker
              ├─ SSRF 防护
              ├─ robots/sitemap
              ├─ 页面发现
              ├─ HTTP 抓取
              ├─ 浏览器渲染回退
              ├─ 内容提取
              ├─ 确定性规则
              ├─ 事实候选
              └─ AI 复核
      │
      ├─ PostgreSQL
      └─ 临时对象存储
```

### 推荐技术边界

- 使用 TypeScript 全栈 Web 框架；
- PostgreSQL 保存扫描、页面和结果；
- 使用简单可靠的任务队列；
- Playwright/Chromium 只作为 JavaScript 页面的回退；
- 对象存储保存临时截图；
- Web 应用与扫描 Worker 分开部署；
- 通过轮询或 Server-Sent Events 更新进度；
- V0 不需要 WebSocket；
- 不需要微服务集群；
- 不需要向量数据库；
- 不需要搜索引擎服务；
- 不需要支付和邮件服务。

---

## 12. 后台处理流程

```text
1. 接收 URL
2. 格式标准化
3. DNS 与 SSRF 安全检查
4. 创建匿名 Scan 和随机结果 token
5. 返回结果链接
6. Worker 读取 robots.txt 和 sitemap
7. 发现并排序页面
8. 最多抓取 25 页
9. 提取正文、链接和结构化事实
10. 执行确定性检查
11. 生成跨页面冲突候选
12. 对少量候选执行 AI 复核
13. 合并重复问题并排序
14. 标记扫描完成
15. 前端展示结果
16. 7 天后自动清理正文和截图
```

### 任务幂等

- 同一个扫描任务重复执行不能重复创建页面；
- 同一问题必须有稳定 fingerprint；
- 前端重复提交不能同时启动多个扫描；
- Worker 崩溃后可以从安全阶段重试；
- AI 请求失败不能让整个扫描失败。

---

## 13. SSRF 与抓取安全

公开 URL 输入是 V0 最大的安全风险，以下能力必须在上线前完成：

- 只允许 HTTP/HTTPS；
- 禁止 localhost；
- 禁止环回、私有、链路本地和保留网段；
- 禁止云服务 metadata 地址；
- DNS 解析后校验实际 IP；
- 每次重定向都重新校验；
- 防止 DNS rebinding；
- 限制目标端口；
- 限制响应体大小；
- 限制页面数量、深度和总请求数；
- 限制单站并发与总扫描时间；
- 浏览器 Worker 与内部网络隔离；
- 不下载或执行文件；
- 不提交表单；
- 不点击可能改变状态的按钮；
- 不绕过登录、验证码或反爬措施；
- 遇到 429 停止并退避；
- 使用明确 User-Agent；
- 遵守 robots.txt。

SSRF 防护不能通过简单字符串判断实现，必须在 DNS 解析和每次重定向后检查目标地址。

---

## 14. 结果排序

结果优先级建议由以下因素决定：

- 确定性；
- 页面重要性；
- 问题类型；
- 影响页面数；
- AI 置信度。

排序示例：

1. 价格页无法访问；
2. 退款入口返回 404；
3. 价格页与帮助中心出现高置信度价格冲突；
4. 首页存在未替换模板变量；
5. 联系信息疑似不一致；
6. 博客中存在疑似过期活动日期；
7. 普通页面缺少 H1。

不要把“缺少 H1”排在真实价格冲突之前。

---

## 15. 开发计划

## 第 1 周：输入网址并完成后台扫描

### 开发内容

- 首页网址输入；
- URL 标准化；
- SSRF 基础防护；
- 创建匿名扫描；
- 随机结果 token；
- 任务队列；
- robots 和 sitemap；
- 页面发现；
- HTTP 抓取；
- 扫描进度；
- 页面结果存储。

### 退出标准

- 用户无需登录即可提交网站；
- 提交后立即获得结果链接；
- 刷新后仍能看到进度；
- 10 个测试网站中至少 8 个完成扫描；
- 不会访问私网目标；
- 不会无限发现页面。

## 第 2 周：生成可用报告

### 开发内容

- 失效页面；
- 失效站内链接；
- 日期候选；
- 联系信息候选；
- 占位内容；
- 事实抽取；
- 跨页面候选分组；
- 结果页；
- 问题证据；
- 重复问题合并。

### 退出标准

- 固定演示网站的问题能被发现；
- 每条问题都有页面和原文证据；
- 同一失效链接只显示一次；
- 扫描部分失败时仍能展示已有结果。

## 第 3 周：AI 复核、反馈和稳定性

### 开发内容

- AI 结构化复核；
- 严重程度和排序；
- 有帮助/不是问题反馈；
- 复制结果链接；
- 立即删除扫描；
- 7 天自动清理；
- 频率限制；
- 异常请求验证；
- 任务重试；
- 运行监控和错误记录。

### 退出标准

- AI 不可用时仍有确定性报告；
- 结果链接不能被枚举；
- 重复提交不会创建大量任务；
- 数据到期能自动清理；
- 高优先级结果的误报处于可接受范围。

## 第 4 周：真实用户验证，可选

第 4 周不增加功能，只做：

- 邀请陌生用户扫描；
- 人工检查报告；
- 记录误报；
- 优化页面优先级；
- 优化结果文案；
- 回访用户；
- 判断下一步做持续监控还是一次性付费报告。

---

## 16. 固定测试网站

建立一个受控演示网站，包含：

- 首页；
- 价格页；
- 两个功能页；
- 帮助页；
- 联系页；
- 退款政策；
- 两篇博客；
- 一个失效站内链接；
- 两处互相冲突的 Pro 价格；
- 两处不同的免费试用天数；
- 一个已经过去的活动截止日期；
- 一个未替换的模板变量；
- 一个依赖 JavaScript 渲染的页面；
- 一个 robots.txt 禁止页面。

演示结果应将确定性问题和疑似问题明确分开。

---

## 17. 测试清单

### 功能测试

- 输入裸域名；
- 输入带路径的网址；
- 输入无效协议；
- 输入不存在域名；
- sitemap 不存在；
- sitemap index；
- JavaScript 页面；
- 404、500 和超时；
- 重定向循环；
- 外部域名重定向；
- robots 禁止；
- 无限分页；
- 大量 query 参数；
- 动态当前时间；
- Cookie 横幅；
- 同一链接多处失效；
- AI 超时；
- 部分页面失败；
- 用户刷新进度页；
- 用户重复提交；
- 用户删除结果。

### 安全测试

- localhost；
- IPv4 和 IPv6 私网；
- 十进制、十六进制或其他形式表示的 IP；
- DNS 指向私网；
- 公网地址重定向到私网；
- DNS rebinding；
- metadata 地址；
- 非标准端口；
- 超大响应；
- 压缩炸弹；
- 恶意 HTML；
- 下载文件；
- 页面尝试打开大量新窗口；
- 高频重复扫描。

---

## 18. V0 验收标准

### 产品验收

- 首页无需登录；
- 用户只输入一个网址即可开始；
- 提交操作在 2 秒内返回扫描任务和结果链接；
- 25 页以内的网站通常能在 10 分钟内完成；
- 扫描中可以查看进度；
- 刷新后不会丢失任务；
- 结果页直接展示问题，不要求注册；
- 每条问题包含页面和原文证据；
- 可以复制结果链接；
- 可以立即删除结果；
- 结果 7 天后自动失效。

### 检测验收

- 能发现固定测试网站中的 404 和失效链接；
- 能发现明显占位内容；
- 能生成价格和试用期冲突候选；
- 能区分确定问题和疑似问题；
- 博客发布时间不会被直接当作过期问题；
- 同一根因不会重复显示；
- AI 失败不影响确定性检查；
- 部分抓取失败不导致整个报告失败。

### 安全验收

- 无法访问私网、localhost 和 metadata；
- 所有重定向都重新校验；
- 请求数量、页面数量和响应大小均有限制；
- 扫描任务有频率限制；
- 结果 ID 不可预测；
- 过期数据能够清理；
- 浏览器任务无法访问内部基础设施。

---

## 19. 验证指标

建议邀请至少 30 名非团队成员使用，争取获得 15 次有效扫描。

### 使用漏斗

- 打开首页人数；
- 输入有效网址人数；
- 成功创建扫描人数；
- 等待扫描完成或返回查看结果人数；
- 展开至少一条问题人数；
- 点击“有帮助”人数；
- 复制结果链接人数；
- 点击重新扫描或表达持续监控需求人数。

### 建议继续开发的标准

- 至少 70% 的有效扫描能够完成；
- 至少 50% 的完成报告包含一个用户认可的问题；
- 高优先级问题中至少 80% 被认为值得查看；
- 平均每份报告无意义问题不超过 3 条；
- 至少 30% 的用户展开问题证据；
- 至少 20% 的用户分享结果或主动询问持续监控；
- 至少 3 名陌生用户愿意为完整报告或持续监控付费。

### 需要调整方向的信号

- 用户只关心失效链接，不关心内容矛盾；
- 用户认为报告有用，但不愿持续监控；
- 用户只愿意进行一次性体检；
- 小型 SaaS 的页面太少，很少出现问题；
- 代理商比网站所有者表现出更强需求；
- 用户希望先提供“当前正确事实”，而不是完全自动判断。

---

## 20. V0 之后的决策

根据验证结果，只能选择一条主路线继续：

### 路线 A：持续监控 SaaS

适用信号：用户愿意定期重新检查，希望收到变化提醒。

下一步才增加：

- 注册与保存网站；
- 定时扫描；
- 邮件或消息通知；
- 历史快照；
- 团队协作；
- 订阅计费。

### 路线 B：一次性网站体检

适用信号：用户重视首次报告，但不需要持续使用。

下一步增加：

- 更多页面；
- 更完整报告；
- PDF 导出；
- 单次支付；
- 人工复核；
- 代理商批量报告。

### 路线 C：停止或重新定位

适用信号：报告很难发现真实问题，或者误报无法控制。

不要因为已经开发了抓取系统，就继续堆积无关 SEO 功能。

---

## 21. 给开发 AI 的执行顺序

开发 AI 必须按垂直流程实现，不能先创建大量空模块。

### 切片一

首页输入网址 → 创建匿名任务 → 返回随机结果链接 → 展示进度。

### 切片二

安全检查 → 发现页面 → 抓取最多 25 页 → 展示页面清单。

### 切片三

检测 404 和失效站内链接 → 生成结果 → 展示证据。

### 切片四

抽取价格、日期和试用期 → 生成跨页面候选 → 展示双方原文。

### 切片五

AI 复核候选 → 结果排序 → AI 失败时安全降级。

### 切片六

反馈按钮 → 复制结果链接 → 立即删除 → 7 天自动清理 → 防滥用。

每个切片必须完成自动测试和人工演示后再进入下一个切片。

---

## 22. 明确不开发

- 登录和注册；
- 用户账户；
- 团队与权限；
- 保存多个网站；
- 定时扫描；
- 邮件通知；
- 支付和套餐；
- CMS 接入；
- 自动修改网站；
- 登录后页面；
- 用户自定义复杂规则；
- 历史变化趋势；
- Slack、飞书或企业微信；
- PDF 报告；
- 白标；
- 移动 App；
- 大型网站支持；
- 全站 SEO、性能、无障碍或安全审计。

如果某项需求不直接帮助完成“输入网址并获得一次有价值的报告”，就不进入 V0。

---

## 23. 最终交付物

开发完成时必须交付：

1. 无需登录的首页；
2. 匿名扫描结果页；
3. 后台任务与扫描 Worker；
4. 数据库迁移；
5. 固定测试网站或测试夹具；
6. 自动化测试；
7. 本地运行和部署说明；
8. SSRF 防护说明；
9. 数据保留与删除机制；
10. 防滥用和频率限制；
11. V0 验收清单；
12. 已知限制；
13. 用户验证数据记录方式。

---

## 24. 最终版本定义

本次修改后的 V0 可以用一句话准确描述：

> 用户打开 FreshPage 首页，无需登录，只输入一个公开网站网址；后台自动检查最多 25 个页面，并在前端直接展示失效链接、明确内容异常以及疑似过期或冲突的内容，结果匿名保存 7 天。

这版比原方案进一步删除了账户、登录、事实配置、定时扫描和邮件系统，开发难度预计从约 5/10 降低到 **4/10～5/10**。

需要重点投入的部分只剩四项：

1. URL 与 SSRF 安全；
2. 网站抓取稳定性；
3. 结果证据和排序；
4. 误报控制。
